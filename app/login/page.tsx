"use client";
import {useState} from "react";import {useRouter} from "next/navigation";import {createClient} from "@/lib/supabase/client";
export default function Login(){const router=useRouter(),[email,setEmail]=useState(""),[password,setPassword]=useState(""),[message,setMessage]=useState("");async function submit(){try{const sb=createClient();const {error}=await sb.auth.signInWithPassword({email,password});if(error)throw error;setMessage("Acesso realizado. Redirecionando…");router.push("/")}catch(e){setMessage(e instanceof Error?e.message:"Não foi possível continuar.")}}return <main className="grid min-h-screen place-items-center bg-ink p-4"><section className="w-full max-w-md rounded-[2rem] bg-white p-7"><p className="label">Orçamento da Família</p><h1 className="mt-2 text-3xl font-black">Entre na sua família</h1><p className="mt-2 text-sm text-ink/60">Acesso exclusivo para membros convidados da família.</p><label className="mt-6 block"><span className="label">E-mail</span><input autoComplete="email" className="tap mt-1 w-full rounded-xl border px-4" type="email" value={email} onChange={e=>setEmail(e.target.value)}/></label><label className="mt-4 block"><span className="label">Senha</span><input autoComplete="current-password" className="tap mt-1 w-full rounded-xl border px-4" type="password" value={password} onChange={e=>setPassword(e.target.value)} onKeyDown={e=>{if(e.key==="Enter")submit()}}/></label><button onClick={submit} className="tap mt-6 w-full rounded-xl bg-ink font-black text-white">Entrar</button><p className="mt-4 text-center text-xs text-ink/55">Não possui acesso? Solicite um convite ao administrador da família.</p>{message&&<p role="status" className="mt-4 rounded-xl bg-mint p-3 text-sm">{message}</p>}</section></main>}
