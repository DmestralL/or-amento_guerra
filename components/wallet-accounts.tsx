"use client";
import {useCallback,useEffect,useMemo,useState} from "react";
import {ArrowLeft,Check,Landmark,Plus,Save,Trash2,WalletCards,X} from "lucide-react";
import {brl} from "@/lib/format";
import {createClient} from "@/lib/supabase/client";
import type {TransactionItem} from "@/components/transaction-history";

export type AccountItem={id:string;name:string;kind:string;openingBalance:number;available:boolean;valueStatus:string};
type DbAccount={id:string;name:string;kind:string;opening_balance:number|string;available_for_spending:boolean;value_status:string};
const labels:Record<string,string>={checking:"Conta corrente",savings:"Poupança",cash:"Dinheiro",benefit:"Benefício",reserve:"Reserva",asset:"Patrimônio"};
const parseMoney=(value:string)=>Number(value.replaceAll(".","").replace(",","."));

export function WalletAccounts({householdId,txs,back}:{householdId:string;txs:TransactionItem[];back:()=>void}){
 const [accounts,setAccounts]=useState<AccountItem[]>([]),[editing,setEditing]=useState<AccountItem|null>(null),[adding,setAdding]=useState(false),[message,setMessage]=useState("");
 const load=useCallback(async()=>{if(!householdId)return;const {data,error}=await createClient().from("accounts").select("id,name,kind,opening_balance,available_for_spending,value_status").eq("household_id",householdId).order("created_at");if(error){setMessage(error.message);return}setAccounts(((data??[]) as DbAccount[]).map(a=>({id:a.id,name:a.name,kind:a.kind,openingBalance:Number(a.opening_balance),available:a.available_for_spending,valueStatus:a.value_status})))},[householdId]);
 useEffect(()=>{queueMicrotask(()=>void load())},[load]);
 useEffect(()=>{if(!householdId)return;const sb=createClient(),channel=sb.channel(`family-accounts-${householdId}`).on("postgres_changes",{event:"*",schema:"public",table:"accounts",filter:`household_id=eq.${householdId}`},()=>void load()).subscribe();return()=>{void sb.removeChannel(channel)}},[householdId,load]);
 const totals=useMemo(()=>{const opening=accounts.filter(a=>a.available).reduce((s,a)=>s+a.openingBalance,0),income=txs.filter(t=>t.type==="income").reduce((s,t)=>s+t.amount,0),out=txs.filter(t=>(t.type==="expense"||t.type==="debt_payment")&&!t.cardId).reduce((s,t)=>s+t.amount,0),card=txs.filter(t=>t.type==="expense"&&t.cardId).reduce((s,t)=>s+t.amount,0);return{opening,income,out,card,balance:opening+income-out}},[accounts,txs]);
 async function remove(account:AccountItem){if(!confirm(`Excluir ${account.name}?`))return;const {error}=await createClient().from("accounts").delete().eq("id",account.id).eq("household_id",householdId);setMessage(error?error.message:"Conta removida.");if(!error)await load()}
 return <div className="space-y-5"><div className="flex items-center gap-3"><button onClick={back} className="tap focus-ring grid w-12 place-items-center rounded-2xl bg-white" aria-label="Voltar"><ArrowLeft/></button><div><p className="label">Saldo e contas</p><h2 className="text-3xl font-black">Minha carteira</h2></div></div>
  {message&&<div role="status" className="rounded-2xl bg-mint p-4 text-sm font-bold">{message}</div>}
  <section className="rounded-[2rem] bg-ink p-6 text-white shadow-soft"><div className="flex justify-between"><div><p className="text-sm text-white/60">Saldo disponível calculado</p><p className="money mt-1 text-4xl font-black">{brl(totals.balance)}</p></div><WalletCards className="text-lime"/></div><div className="mt-6 grid grid-cols-3 gap-3 border-t border-white/15 pt-5 text-xs"><span>Saldo inicial<br/><b className="money text-base">{brl(totals.opening)}</b></span><span>Entradas<br/><b className="money text-base text-lime">+ {brl(totals.income)}</b></span><span>Saídas<br/><b className="money text-base">− {brl(totals.out)}</b></span></div></section>
  <section className="card p-5"><p className="label">Como o saldo é calculado</p><p className="mt-2 text-sm text-ink/65">Saldo inicial das contas disponíveis + receitas − despesas pagas fora do cartão.</p><div className="mt-3 rounded-xl bg-amber-50 p-3 text-sm"><b>{brl(totals.card)}</b> em compras no cartão não reduz este saldo agora; entra na saída quando a fatura for paga.</div></section>
  <div className="flex items-center justify-between"><h3 className="text-xl font-black">Contas e carteiras</h3><button onClick={()=>setAdding(true)} className="tap flex items-center gap-2 rounded-xl bg-lime px-4 text-sm font-black"><Plus size={17}/> Adicionar</button></div>
  <section className="card divide-y divide-ink/10">{accounts.map(account=><article key={account.id} className="flex items-center gap-3 p-4"><span className="grid h-11 w-11 place-items-center rounded-2xl bg-mint">{account.kind==="cash"?<WalletCards/>:<Landmark/>}</span><div className="min-w-0 flex-1"><b>{account.name}</b><p className="text-xs text-ink/55">{labels[account.kind]||account.kind} · {account.available?"entra no saldo":"separada do saldo"}</p></div><strong className="money">{brl(account.openingBalance)}</strong><button onClick={()=>setEditing(account)} className="tap rounded-lg px-2 text-xs font-bold">Editar</button></article>)}</section>
  {(adding||editing)&&<AccountForm householdId={householdId} account={editing} close={()=>{setAdding(false);setEditing(null)}} saved={async text=>{setAdding(false);setEditing(null);setMessage(text);await load()}} remove={editing?()=>void remove(editing):undefined}/>} 
 </div>
}

function AccountForm({householdId,account,close,saved,remove}:{householdId:string;account:AccountItem|null;close:()=>void;saved:(message:string)=>void;remove?:()=>void}){const [name,setName]=useState(account?.name||""),[kind,setKind]=useState(account?.kind||"checking"),[amount,setAmount]=useState(account?String(account.openingBalance).replace(".",","):""),[available,setAvailable]=useState(account?.available??true),[busy,setBusy]=useState(false),[error,setError]=useState("");async function save(){const value=parseMoney(amount);if(!name.trim()||Number.isNaN(value)){setError("Informe nome e saldo inicial válido.");return}setBusy(true);const sb=createClient(),payload={household_id:householdId,name:name.trim(),kind,opening_balance:value,available_for_spending:available,value_status:"confirmed"};let result;if(account)result=await sb.from("accounts").update(payload).eq("id",account.id).eq("household_id",householdId);else{const {data:{user}}=await sb.auth.getUser();result=await sb.from("accounts").insert({...payload,created_by:user?.id})}setBusy(false);if(result.error){setError(result.error.message);return}await saved(account?"Conta atualizada e saldo recalculado.":"Conta adicionada e saldo recalculado.")}return <div className="fixed inset-0 z-50 flex items-end justify-center bg-ink/45 sm:items-center sm:p-4"><div className="w-full max-w-lg rounded-t-[2rem] bg-white p-5 sm:rounded-[2rem]"><div className="flex justify-between"><div><p className="label">{account?"Editar":"Nova"}</p><h3 className="text-2xl font-black">Conta ou carteira</h3></div><button onClick={close} className="tap grid w-12 place-items-center"><X/></button></div><div className="mt-4 grid gap-3 sm:grid-cols-2"><label className="sm:col-span-2"><span className="label">Nome</span><input value={name} onChange={e=>setName(e.target.value)} placeholder="Ex.: Conta Nubank" className="tap mt-1 w-full rounded-xl border px-3"/></label><label><span className="label">Tipo</span><select value={kind} onChange={e=>setKind(e.target.value)} className="tap mt-1 w-full rounded-xl border bg-white px-3">{Object.entries(labels).map(([value,label])=><option key={value} value={value}>{label}</option>)}</select></label><label><span className="label">Saldo inicial</span><input inputMode="decimal" value={amount} onChange={e=>setAmount(e.target.value)} className="tap mt-1 w-full rounded-xl border px-3"/></label><label className="flex items-center gap-2 sm:col-span-2"><input type="checkbox" checked={available} onChange={e=>setAvailable(e.target.checked)}/><span className="text-sm font-bold">Disponível para gastar</span></label></div>{error&&<p className="mt-3 text-sm font-bold text-red-700">{error}</p>}<div className="mt-4 flex gap-2">{remove&&<button onClick={remove} className="tap grid w-12 place-items-center rounded-xl border text-red-700" aria-label="Excluir"><Trash2 size={17}/></button>}<button disabled={busy} onClick={()=>void save()} className="tap flex flex-1 items-center justify-center gap-2 rounded-xl bg-ink font-bold text-white"><Save size={17}/>{busy?"Salvando…":"Salvar e recalcular"}</button></div><p className="mt-3 flex items-center gap-2 text-xs text-ink/55"><Check size={14}/> Alterações sincronizadas com a família.</p></div></div>}
